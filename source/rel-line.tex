%!TEX root = io2d.tex
\rSec0 [\iotwod.relline] {Class \tcode{rel_line}}

\pnum
\indexlibrary{\idxcode{rel_line}}%
The class \tcode{rel_line} describes a figure item that is a segment.

\pnum
It has an \term{end point} of type \tcode{basic_point_2d}.

\rSec1 [\iotwod.relline.cons] {\tcode{rel_line} constructors}

\indexlibrary{\idxcode{rel_line}!constructor}%
\begin{itemdecl}
rel_line() noexcept;
\end{itemdecl}
\begin{itemdescr}
\pnum
\effects
Equivalent to: \tcode{rel_line\{ basic_point_2d() \};}
\end{itemdescr}

\indexlibrary{\idxcode{rel_line}!constructor}%
\begin{itemdecl}
explicit rel_line(const basic_point_2d<typename
  GraphicsSurfaces::graphics_math_type>& pt) noexcept;
\end{itemdecl}
\begin{itemdescr}
\pnum
\effects
Constructs an object of type \tcode{rel_line}.

\pnum
The end point is \tcode{pt}.
\end{itemdescr}

\indexlibrary{\idxcode{rel_line}!constructor}%
\begin{itemdecl}
rel_line(const rel_line& other);
rel_line(rel_line&& other) noexcept;
\end{itemdecl}
\begin{itemdescr}
\pnum
\effects
Constructs an object of type \tcode{rel_line}. In the second form, other is left in a valid state with an unspecified value.

\pnum
The end point is \tcode{other.to()}.
\end{itemdescr}

\rSec1 [\iotwod.relline.assign] {\tcode{rel_line} assignment operators}

\indexlibrary{\idxcode{rel_line}!assignment}%
\begin{itemdecl}
rel_line& operator=(const rel_line& other);
\end{itemdecl}
\begin{itemdescr}
\pnum
\effects
If \tcode{*this} and \tcode{other} are not the same object, modifies \tcode{*this} such that \tcode{*this.to()} is \tcode{other.to()}

\pnum
If \tcode{*this} and \tcode{other} are the same object, the member has no effect.

\pnum
\returns
\tcode{*this}
\end{itemdescr}

\indexlibrary{\idxcode{rel_line}!assignment}%
\begin{itemdecl}
rel_line& operator=(rel_line&& other) noexcept;
\end{itemdecl}
\begin{itemdescr}
\pnum
\effects
<TODO>

\pnum
\returns
\tcode{*this}
\end{itemdescr}

\rSec1 [\iotwod.relline.modifiers]{\tcode{rel_line} modifiers}

\indexlibrarymember{rel_line}{to}
\begin{itemdecl}
void to(const basic_point_2d<typename GraphicsSurfaces::graphics_math_type>& pt) noexcept;
\end{itemdecl}
\begin{itemdescr}
\pnum
\effects
The end point is \tcode{pt}.
\end{itemdescr}

\rSec1 [\iotwod.relline.observers]{\tcode{rel_line} observers}

\indexlibrary{\idxcode{rel_line}!\idxcode{to}}%
\begin{itemdecl}
basic_point_2d<typename GraphicsSurfaces::graphics_math_type> to() const noexcept;
\end{itemdecl}
\begin{itemdescr}
\pnum
\returns
The end point.
\end{itemdescr}

\rSec1 [\iotwod.relline.ops]{\tcode{rel_line} operators}

\indexlibrarymember{operator==}{rel_line}%
\begin{itemdecl}
template <class GraphicsSurfaces>
bool operator==(const typename basic_figure_items<GraphicsSurfaces>::rel_line& lhs,
  const typename basic_figure_items<GraphicsSurfaces>::rel_line& rhs) noexcept;
\end{itemdecl}
\begin{itemdescr}
\pnum
\returns
\tcode{lhs.to() == rhs.to()}.
\end{itemdescr}
